fun handleForm(p) {
	do something
}
fun mainPage (_) {
  page
    <html>
    <body>
      <h1>Example form</h1>
      <form l:action="{handleForm(i)}">
		<input type="checkbox" l:name="i" /> Test
        <button type="submit">Submit!</button>
      </form>
      </body>
    </html>
}

